mixin navbar
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link href="https://fonts.googleapis.com/css2?family=Indie+Flower&family=Londrina+Solid&display=swap" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@500&family=Indie+Flower&family=Londrina+Solid&display=swap" rel="stylesheet">
  div.navbar
    div.navbar__logo
      a(href="/")
        div.logo__1 COMIC
        div.logo__2 Collection
    div.site-nav
      .nav__child.nav__home.active
        a(href="/") Home
      .nav__child.nav__collections
        if locals.authenticated
            a(href=`/collections`) My Collections
        else
          a(href="/login") My Collections
      div.dropdown.nav__child
        button.dropbtn Browse
          i(class="fa fa-caret-down")
        div.dropdown-content
          p FAVORITE GENRES
          a(href="") Fantasy
          a(href="") Fiction
          a(href="") Superhero
      div.dropdown.nav__child
        button.dropbtn Community
        div.dropdown-content
          a(href="") Discussions
          a(href="") People
          a(href="") Ask the Author
    div(class="search-bar" id="search")
          //-  form(action="/search" method="GET")
          input.search-bar__text(type="text" placeholder="search comics" name="searchBar" id="searchBar")
          //- button.search-bar__submit(type="submit")
          //-   ion-icon(name="search-outline")
          //- button(type="submit" class="search-btn")
    ul(id="comicsList")
    if locals.authenticated
      div.personal-nav
        div(class="dropdown" id="notify-div")
          button(class="dropbtn" id="notify-btn") 🔔 Notifications
          i(class="fa fa-caret-down")
          div.dropdown-content
            a(href="") No notifications
        a(href="") 💬 Chat
        a(href="") Messages
        a(href="") Friends
        div.dropdown
            img(src="https://s.gr-assets.com/assets/nophoto/user/u_60x60-267f0ca0ea48fd3acfd44b95afa64f01.png")
            form(action="/users/logout" method="POST")
              div.dropdown-content
                button(class="sign-out-btn" type="submit") Sign out
    else
      //- a(href="/signup") Sign Up
      //- a(href="/login") Log In
      div.dropdown
        button.dropbtn Start
          i(class="fa fa-caret-down")
          div.dropdown-content
            div(class="demo_user")
              form(action="/users/demo" method="POST")
                input(type="submit" value="Demo User")
            a(href="/signup") Sign Up
            a(href="/login") Log In
  <script src="https://unpkg.com/ionicons@5.0.0/dist/ionicons.js"></script>
